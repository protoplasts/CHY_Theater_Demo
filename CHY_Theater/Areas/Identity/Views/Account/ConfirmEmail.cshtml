@section Styles {
    <style>
        body {
            background-color: #000; /* Black background */
            color: #fff; /* White text color for contrast */
            font-family: Arial, sans-serif; /* Modern, clean font */
        }

        .confirm-container {
            padding-top: 70px; /* Adjust based on your navbar height */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 70vh; /* Full viewport height */
        }

        .confirm-box {
            background-color: #1a1a1a; /* Dark gray background for contrast */
            padding: 40px;
            border-radius: 10px; /* Rounded corners */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3); /* Subtle shadow for depth */
            max-width: 500px; /* Limit the width for better readability */
            text-align: center;
        }

        h1 {
            font-size: 36px; /* Larger, prominent heading */
            margin-bottom: 20px;
        }

        p {
            font-size: 18px; /* Slightly larger paragraph text */
        }

        a {
            color: #4CAF50; /* Bright green link color */
            text-decoration: none; /* Remove underline */
            font-weight: bold; /* Bold the link */
        }

            a:hover {
                color: #8BC34A; /* Slightly brighter green on hover */
                text-decoration: underline; /* Underline on hover */
            }
    </style>
}

<div class="container confirm-container">
    <div class="confirm-box">
        <h1>電子郵件已確認</h1>
        <p>
            您的電子郵件已成功確認。請<a asp-controller="Account" asp-action="Login">點擊此處登入</a>。
        </p>
    </div>
</div>
